
        /*
        event.preventDefault()
        
        const formdata = new FormData(dom_form)
        fetch(action, {
            method: "POST",
            body: formdata
        })
        .then(response => {
            if (!response.ok)
                throw new Error('network issue', response)
            return response.json()
        })
        .then(data => {
            utils.cl(data.data)
            form_message_okay = data.data
        })
        .catch(error => {
            form_message_error = error
        })
        */